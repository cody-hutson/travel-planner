---
artifact: groups/<group>.md
schema-version: 1
trip: cross-trip
writer: operator
lifecycle: persist-mutable
provenance: human
publish: internal-hard
---

# Ski crew

> **Illustrative, sanitized example. Not a real group of people.** No real personal
> detail appears in this file, and none ever may: this class is `publish: internal-hard`
> and every real instance of it is git-ignored. This one is tracked **only** because the
> schema gate needs a witness it can reach.

**The H1 is the display name and nothing else.** Not `# Group — Ski crew`, not a decorated
title: the normalized H1 is the key a creation collision is refused on, and any decoration
would enter that normalization and change the key. The group's **id** is the filename stem,
`grp-4a81`, and it appears nowhere inside this file — restating it here would give one value
a second home.

**Everything below this line is the record.** The two blocks above are what a reader needs to
know that this file is a fixture; the record itself is the H1 and the section that follows,
and nothing else. A real record carries neither of them.

## Members

- psn-3c7e
- psn-9d42
